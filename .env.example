# Leave LLM_MODE=fixture to run the pipeline with stored model output and no API key.
# Set LLM_MODE=live and supply a key to call the model for real.
LLM_MODE=fixture
# OPENROUTER_API_KEY=sk-or-...
# LLM_MODEL=openai/gpt-4o-mini
PORT=3000
